// Archivo: /api/index.js (Entrypoint para Vercel)
// Este archivo es el punto de entrada que Vercel busca por defecto en la ruta /api

const app = require('../backend/js/app'); // Asegúrate de que esta ruta sea correcta

module.exports = app;